{% block content %}
<nav class="navbar navbar-expand-md navbar-dark bg-dark">
    <div class="container-fluid">
        <a href="/" class="navbar-brand">HealthApp</a>
        {% if loggedIn %}

        {% endif %}
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            {% if loggedIn %}
                <a href="/dashboard" class="nav-link text-light">Find a pharmacy</a>
                <a href="/med/my-meds" class="nav-link text-light">My Meds</a>
                <a href="/glucose/my-glucoses" class="nav-link text-light">My Glucose</a>

            {% else %}
                <a href="/auth/signup" class="nav-link text-light">Sign Up</a>
                <a href="/auth/login" class="nav-link text-light">Sign In</a>
            {% endif %}
        </div>
        <div class="nav-link text-light">Welcome back , {{ username }} </div>
        <a href="/auth/logout" class="nav-link text-light">Log Out</a>
    </div>
</nav>
{% endblock %}