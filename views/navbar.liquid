{% block content %}
<nav class="navbar navbar-expand-md navbar-dark" style="background-color: rgb(88, 88, 88);" >
    <div class="container-fluid">
        <div class="navbar-brand">HealthApp</div>
        {% if loggedIn %}

        {% endif %}
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        {% if loggedIn %}
        <div class="collapse navbar-collapse" id="navbarNav">
        <a href="/med/pharmacy" class="nav-link text-light">Find a pharmacy</a>
        <a href="/med/my-meds" class="nav-link text-light">My Meds</a>
        <a href="/glucose/my-glucoses" class="nav-link text-light">My Glucose</a> <br>
    </div>
        <div class="nav-link text-light" id="nav-right">Welcome back , {{ username }} </div>
        <a href="/auth/logout" class="nav-link text-light" id="nav-right">Log Out</a>

            {% else %}
                <a href="/auth/signup" class="nav-link text-light" id="sign">Sign Up</a>
                <a href="/auth/login" class="nav-link text-light" id="sign">Sign In</a>
            {% endif %}
    </div>
</nav>
{% endblock %}